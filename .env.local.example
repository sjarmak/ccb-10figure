# CodeContextBench Environment Configuration
#
# Copy this file to .env.local and fill in your actual credentials.
# NEVER commit .env.local to version control.
#
# Usage:
#   cp .env.local.example .env.local
#   source .env.local
#   python runners/harbor_benchmark.sh --benchmark 10figure --agent claude-baseline

# === Required: Claude API ===
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY="sk-ant-..."

# === Required for Claude+MCP agent: Sourcegraph Token ===
# Get from: https://sourcegraph.sourcegraph.com/user/settings/tokens
SRC_ACCESS_TOKEN="sgp_..."

# === Optional: Sourcegraph Instance ===
# Defaults to: https://sourcegraph.sourcegraph.com
SOURCEGRAPH_URL="https://sourcegraph.sourcegraph.com"

# === Optional: 10Figure Corpus Path ===
# Location of the 10Figure dataset (local development)
# Defaults to: /10figure (in Harbor containers)
HARBOR_10FIGURE="/Users/sjarmak/harbor-10figure-dataset"

# === Optional: Container Runtime ===
# Options: podman (default), docker
CONTAINER_RUNTIME="podman"

# === Optional: Development Flags ===
# Uncomment for verbose benchmark execution
# DEBUG="1"
# VERBOSE="1"
